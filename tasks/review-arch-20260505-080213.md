# Architecture Review — image-regeneration + directive cleanup

**Tidpunkt:** 2026-05-05 08:02:13
**Reviewer:** Claude opus (general-purpose)
**Diff-range:** 3f40236..1567aa7

---

## Sibling context

Files I read to learn local patterns:

- `/Users/daniel/projects/arduino-kurs/CLAUDE.md` — establishes the two-subsystem layout, atomic-commit rule, image-regeneration/light-mode invariants, pedagogically protected colors registry, and verification pipeline.
- `tasks/2026-05-05-image-regeneration-plan.md` — the plan being executed; defines style baseline, prompt prefix, batch organization, originals-backup directory, and explicit out-of-scope (Fritzing PNGs, 6 orphaned images, kompendium).
- `tasks/2026-05-05-light-mode-plan.md` (head) — taught me how prior plans here documented atomic commit boundaries.
- `tasks/2026-05-05-light-mode-design.md` — taught me the rationale for the `voltage-divider.svg` having a "naturally-light asset that got hacked dark" problem.
- `presentation/slides.md` (frontmatter, slides 8, 10, 11, 12, 14, 19, 21, 24, 25, 26, 39-40, 41, 42, 50) — confirmed pedagogical-color invariants (resistor bands `#d4a574/#ff2222/#8b4513/#d4af37`, RGB-LED gradients via `.rgb-circle` classes, RGB chip examples `#b400dc/#ff8cb4/#ffd400/#00e0ff`) are still in place.
- `presentation/styles/main.css` — taught me conventions of class registry (sizing + soft shadow over hardcoded backgrounds), `--ak-*` token usage, `html.print` overrides.
- `kompendium/` (top-level + deliveries) — confirmed zero cross-reference from kompendium Typst sources to presentation image filenames.
- `presentation/public/images/` listing — confirmed file placements.

I also ran `npm run build` from `presentation/`: builds cleanly in 6.11s with no warnings about missing assets.

## Strengths

**Boundary discipline is exemplary.** All edits live under `presentation/` and `tasks/`. Zero changes leaked into `kompendium/`. Verified via `grep -rln`.

**Layer separation is intact.** Image binaries → `public/images/`; slide refs → `slides.md`; CSS classes → `main.css`; source generation artifacts → `tasks/image-gen/v1/` (raw outputs) and `tasks/image-gen/originals/` (pre-swap backups). Exactly the topology the plan specified.

**`.rgb-demo` simplification is the right shape.** The metaphor moved into the image itself, so removing wrapper bg + padding is structurally correct: CSS no longer simulates a layered context; image is self-contained. Comment update at line 280-282 documents the new ownership clearly.

**Plan→commit→artifact traceability.** Plan enumerates 13 images; `tasks/image-gen/v1/` contains exactly those 13 (plus retake + style-validation); `presentation/public/images/` shows corresponding 13 modifications/additions. Future contributor can reproduce work end-to-end.

**Commit `ad582f5` is the right shape for "process artifacts."** `docs(tasks):` type, explanatory message body, includes both originals and v1 sources. Type prefix correctly signals "no functional/visible change to delivered artifact."

**Atomic commit boundaries enable selective rollback.** Commit `2dc8765` touches *only* `slides.md` (1 file, 56 ± lines). A `git revert 2dc8765` would cleanly undo directive cleanup without disturbing image work.

**Plan scope-discipline holds.** Out-of-scope items (Fritzing PNGs, 6 orphaned images, kompendium) verified untouched.

**Pedagogical color invariants intact across the diff:**
- Slide 10 resistor bands `#d4a574 #ff2222 #8b4513 #d4af37` — present at `slides.md:549-554` ✓
- Slide 19/26 RGB-LED-circles via `.rgb-circle` — at `main.css:257-260`; usages at `slides.md:1077-1079, 1402-1404` ✓
- Slide 25 RGB chip examples `#b400dc #ff8cb4 #ffd400 #00e0ff` — present at `slides.md:1340-1343, 1355-1358` ✓
- Sun symbol `#ffd400` was inside the deleted `voltage-divider.svg`. Per plan #6, regenerated PNG includes the sun explicitly via prompt: *"a small soft sun icon (#ffd400 yellow) gently illuminating it from above"*. Invariant transferred into AI prompt.

## Issues

### Critical (must fix)

**None.** Structurally sound. No layering violations, no cross-subsystem leakage, no lost pedagogical constants.

### Important (should fix)

**1. `kompendium/deliveries/slides-per-dag/` per-day PDFs are stale and not flagged.**
- `slides-dag-{1..5}.pdf` (dated 28 Apr) are 8 days older than the new content. Slide content changed (directive cleanup edits visible text on slides 12 and 25 + image swaps on 8, 11, 14, 21, 24, 28, 33, 35-37, 39-40, 41).
- CLAUDE.md "Out-of-scope för Slidev-arbete" explicitly says these *"behöver re-genereras post-merge om slides ändras radikalt"*.
- **Why it matters structurally:** Deliverable PDFs now visually out-of-date with source. They were git-tracked before this diff and remain git-tracked unchanged.
- **Fix:** Add a one-line follow-up either as new `tasks/2026-05-05-followups.md` or appended to image-regeneration-plan's out-of-scope section.

**2. Originals-backup commit ordering inverts the safety pattern.**
- Commit `9dd9703` (image regen + delete `voltage-divider.svg` + delete `ohms-law-cartoon.jpg`) is committed *before* `ad582f5` (originals backup). In practice not a data-loss issue here because backup bytes are intact, but the *narrative* is "delete original, then save it as backup" rather than "back up first, then swap."
- **Why it matters structurally:** Plan step 7 explicitly calls for *"Backup originalbilder → tasks/image-gen/originals/"* before *"Atomic commit per bild-byte"* (step 8). For future image-regeneration sessions following this template, the safer sequence is `docs(tasks):` originals-backup commit first, then `feat(images):` swap.
- **Fix (next round):** Re-order steps in plan's exekvering section. Or document the canonical image-asset-replacement procedure in CLAUDE.md.

**3. Directive cleanup commit's scope is broader than CLAUDE.md commit-message convention typically allows.** — Single `refactor(slides):` touching both visible tip-boxes AND 8 distinct presenter-notes. CLAUDE.md atomic-commit guidance: *"En commit per logisk enhet"*. By strict reading, "8 slide-fixes in one commit." However, sibling history shows multi-slide policy sweeps as one commit is the codebase pattern (e.g. `b563897`, `92353c3`). **Not a violation, but worth flagging as interpretation choice the codebase has accepted.** No fix needed.

### Minor (nice to have)

**4. The orphaned `.diagram-img` class in `main.css:288-292` is no longer referenced from `slides.md`.** — Class still defined. Dead CSS introduced by side effect. Class definition is small (5 lines) and harmless; CLAUDE.md "Out-of-scope" includes a "separate städ-task om någonsin" line for orphans.
- **Fix (optional):** Either delete the block or leave for eventual orphan-cleanup task.

**5. `tasks/image-gen/originals/wiring/` mirror-backs the entire `presentation/public/images/wiring/` directory (11 files) even though only 3 wiring images were swapped.** — Files like `button-fritzing.png`, `buzzer-fritzing.png`, etc. are in `originals/wiring/` despite not being swapped (out-of-scope per plan). Harmless redundancy.
- **Fix (optional):** Trim to just the 3 swapped files, or add `originals/README.md` explaining mirror policy.

**6. Plan's status table** (`tasks/2026-05-05-image-regeneration-plan.md:23-28, 41-46, 60-64`) still says `pending` for items that completed. Future contributor reading plan as a checklist would not realize this round is done.
- **Fix (optional):** Update those rows to `done`.

**7. `tasks/image-gen/v1/cover-arduino-hero-v2.png` was checked in alongside `cover-arduino-hero.png` without commentary about which one was used.** Minor traceability gap.
- **Fix (optional):** Add note: "v2 was selected; v1 retained as failed-attempt reference."

## Assessment

**Ready to merge?** Yes — with one optional fix (issue #1, the per-day PDFs follow-up flag).

**Reasoning:** Structurally exemplary work. Two-subsystem boundary holds (zero kompendium leakage), all four pedagogical color invariants survive the swap, the SVG sun-symbol metaphor was carried into the AI-prompt for the replacement PNG, the CSS simplification follows the "sizing + shadow" pattern, originals are preserved with auditable bytes, build passes clean (6.11s, no missing-asset warnings). The four commits each represent one cohesive logical unit and enable selective revert. The only structural gap is that the stale `kompendium/deliveries/slides-per-dag/` PDFs aren't flagged anywhere durable. Confidence: **high** that this can merge as-is; **medium-high** that issue #1 should be filed as a follow-up before the next student delivery.

---

## Out of scope but flagging

Code-quality items the parallel reviewer should consider:

- `presentation/slides.md:1311` — the inline `class="h-96"` on the PWM `<img>` is fine but inconsistent with most other image refs that include sizing in `style=` instead. Not a bug; consistency note.
- `presentation/slides.md:2200` — the SVG-replacement PNG has the same `alt` text as the SVG had. Verify the AI-generated PNG actually visually matches the alt's specifics ("+5V → fotocell → A0 → 1 kΩ → GND med typiska värden") — if the prompt didn't reproduce typed values, alt text becomes misleading. A11y verification rather than structural.
- `tasks/image-gen/originals/voltage-divider.svg` shows `Bin 0 -> ...` in the rename-detection diff but is a text SVG. Cosmetic git artifact only.
