# Code Quality Review — image-regeneration + autonomy-policy cleanup

**Tidpunkt:** 2026-05-05 08:02:13
**Reviewer:** Claude opus (general-purpose)
**Diff-range:** 3f40236..1567aa7

---

## Strengths

- **Image-reference swaps are correct and consistent.** All three swaps (`presentation/slides.md:405` ohms-law, `:1311` PWM, `:2200` voltage-divider) point to files that exist on disk (`presentation/public/images/ohms-law.png`, `pwm-waveform.png`, `voltage-divider.png`) and survive a fresh `npm run build` — the build emitted them into `presentation/dist/images/`. No broken image refs.
- **Alt-text preserved correctly.** The voltage-divider swap kept its existing `alt="Spänningsdelare-schema: +5V → fotocell → A0 → 1 kΩ → GND med typiska värden"` verbatim despite the file extension change (`presentation/slides.md:2200`). The other two swapped tags didn't have alt before, so nothing was dropped.
- **Width adjustment on voltage-divider is sensible.** `width: 800px` → `600px` (`presentation/slides.md:2200`) accounts for the higher-resolution PNG vs the previous SVG, prevents the schema from dominating the slide.
- **`.rgb-demo` simplification is cleanly done.** Old comment ("svart frame är pedagogisk metafor") was correctly *replaced* (not appended to) with a comment that matches the new behavior ("Bilden har egen mörk bakgrund som pedagogisk metafor"). The two now-obsolete declarations (`background: #000;` and `padding: 1rem;`) were both removed; the remaining `width`/`border-radius`/`box-shadow` are all genuinely used (`presentation/styles/main.css:280-286`). No stale declarations.
- **Directive cleanup retains pedagogical intent and reads naturally in Swedish.** Each rewrite kept the surrounding context coherent — e.g. `presentation/slides.md:666-668` ("När en LED inte lyser är felkällan nästan alltid att kabel och komponentben hamnat på olika rader") replaces the original directive while *preserving* the same pedagogical point. Same quality on `:939-940` ("Tips för bygget" with `const int` substantive content), `:1057-1064` (VISNINGSRUNDA description), `:1379-1382` (KAMRATSPRIDNING reframed as natural emergence), `:2599-2603` (engineering-life monologue), and `:2698-2702` (closing).
- **The forbidden-phrase grep returns zero hits.** Running the project policy regex `(ropa|säg till|räck upp|om du fastnat|fråga läraren|kalla på|jag kommer förbi|jag går runt|jag finns här|erkännandet från läraren)` against `presentation/slides.md` returns nothing. Policy fully applied.
- **Atomic commit hygiene.** The four-commit split is textbook: feat (binaries) → refactor (text) → docs (plan) → chore (rendered artifacts). Per CLAUDE.md commit policy.

## Issues

### Critical (must fix)

None. No broken image refs, no broken build, no missed forbidden directives.

### Important (should fix)

**1. `.diagram-img` is now dead code.**
- **Where:** `presentation/styles/main.css:288-292` (CSS rule) versus zero callsites in `presentation/slides.md` after the PWM hunk removed `class="h-96 p-3 diagram-img"` → `class="h-96"`.
- **What's wrong:** Verified via `grep -rn "diagram-img" presentation/` — only the CSS definition remains. The class had a single user (the PWM image), and that user is gone. The ruleset survives unused.
- **Why it matters:** CLAUDE.md sektion "Custom CSS-klass-registry" (line 86) explicitly lists `.diagram-img` as Slide 24's "generic diagram-ram med token-border". The registry is a project contract — silent drift here means future readers will believe the class is in use and add new images expecting that styling.
- **Fix:** Either (a) delete the rule from `main.css:288-292` and remove the row from CLAUDE.md's class registry table, or (b) re-add `class="diagram-img"` to the PWM `<img>` if the framed style was pedagogically intended. Recommend (a) — the new PWM render presumably looks fine without the frame, and dead CSS is worse than tightening scope.

**2. CLAUDE.md class registry is out of sync with the actual `.rgb-demo` rule.**
- **Where:** `CLAUDE.md:61` ("Pedagogiskt skyddade konstanter" table: `#000 .rgb-demo bg | main.css:281 | "Mörkrum"-metafor för RGB-LED-demo`) and `CLAUDE.md:85` ("Custom CSS-klass-registry" table: `.rgb-demo | 21 | Svart frame runt RGB-blandnings-bild ("mörkrum/skärmdemo")`).
- **What's wrong:** The diff removed `background: #000;` from `.rgb-demo` and rewrote the comment to say the dark bg now lives *inside the image*. The CLAUDE.md "protected constants" row therefore points to a value (`#000`) and a line number (`main.css:281`) that no longer exists, and the class-registry row still describes the class as a "Svart frame".
- **Why it matters:** The protected-constants table is the project's documented contract for "rör aldrig". A future agent reading it will think `#000` is locked-in even though the diff intentionally removed it.
- **Fix:** Update both rows in CLAUDE.md: remove the `#000` row from protected-constants table; reword `.rgb-demo` registry row to "Sizing + soft shadow för RGB-blandnings-bild (bilden har egen mörk bg)".

**3. Inline `tip-box` content on slide ~939 contains literal Swedish prose with `<code>` tag — lightly inconsistent with how `<code>` is used in surrounding tips.**
- **Where:** `presentation/slides.md:940` — `<div>Lägg tider i <code>const int</code>-variabler...</div>`.
- **What's wrong:** Most surrounding tips use Markdown backticks. CLAUDE.md DO/DON'T table sanctions both, so technically allowed. Low confidence on whether to flag.
- **Fix (optional):** Replace `<code>const int</code>` with `` `const int` ``. Won't affect rendering meaningfully.

### Minor (nice to have)

**4. Mantra rewrite at slides.md:666-668** — "kabel och komponentben hamnat på olika rader" — verb form `hamnat` (perfect participle) without `har` works in colloquial Swedish but might benefit from `har hamnat` for crispness. Not a bug.

**5. slides.md:2702** — "Stående ovation. Något byggt från noll förtjänar det." Replacement removed imperative + subject leaving noun-phrase fragment. Style only; autonomy policy satisfied; informal but acceptable in presenter-note context.

**6. tasks/2026-05-05-image-regeneration-plan.md added by ad582f5** — out of scope for this code review. Mentioned only because diff includes it.

## Assessment

**Ready to merge?** With fixes.

**Reasoning:** The slide-content and CSS edits are clean, the build is green, all image refs resolve, and the policy-driven directive cleanup is grammatically natural and pedagogically intact. However, **two non-blocking but real correctness gaps** remain: `.diagram-img` is now dead code with a stale CLAUDE.md registry entry, and the `.rgb-demo` simplification left two CLAUDE.md rows pointing at deleted state (line number, `#000` value, and "Svart frame" description). These should be reconciled in the same delivery — either remove the dead CSS rule and update the two registry tables, or commit a follow-up `docs(claude-md): sync class registry with rgb-demo + diagram-img cleanup`. Confidence: **high** on the dead-code finding (verified via grep), **high** on the CLAUDE.md drift (verified via line numbers).

---

## Out of scope but flagging

- **Asset weight:** `breadboard-internals.png` jumped from 142 KB → 5.0 MB, `tilt-photo.png` from 247 KB → 5.4 MB, `buzzer-photo.png` 1.7 MB → 5.5 MB. Total `public/images/` is now ~50 MB. May slow `npm run dev` cold-load and bloat deployable. Architectural/perf concern.
- **Originals retained as `.svg`/old-`.jpg` under `tasks/image-gen/originals/`:** good practice for rollback, but increases repo size. Worth verifying with the user whether `tasks/` should be in `.gitignore` for binaries or whether retention is wanted.
- **The `tip-title` in `slides.md:939` was renamed from "Så här jobbar vi" → "Tips för bygget".** Pedagogically fine. Worth a sanity-check render of the slide as part of the visual gate.
