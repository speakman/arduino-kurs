# Architecture Review — light-mode-konvertering

**Tidpunkt:** 2026-05-05 01:46:40
**Reviewer:** Claude opus (general-purpose)
**Diff-range:** b563897..d3dd82e

---

### Sibling context

Files read:

- **`presentation/styles/main.css`** (314 lines, full read) — Single token-driven stylesheet. Numbered section convention. All design tokens in `:root`, consumed via `var(--ak-*)`. Domain components (`.sp-grid`, `.warn-box`, `.tip-box`) defined here. **`.hex-chip` is mentioned in CLAUDE.md but does not exist** — actual class is `.color-chip`.
- **`presentation/setup/shiki.ts`** (40 lines) — Slidev plugin pattern. Sets BOTH `--shiki-light` and `--shiki-dark` per-token. Confirms project's "set both shiki vars" policy.
- **`presentation/uno.config.ts`** (32 lines) — Pure typography rebase for canvasWidth: 1920. Boundary: UnoCSS = type scale only; main.css = colors + components.
- **`presentation/components/WiringSlide.vue`** (44 lines) — utility-only template, no scoped styles. Consumes tokens via `ring-[var(--ak-border)]`.
- **`presentation/slides.md`** (slides 1, 8, 11, 21, 24, 25, 33 + general) — Pattern is *Markdown body + inline HTML islands + UnoCSS utility classes + custom CSS classes from main.css*. Mature dual-pattern.

### Strengths

- **`presentation/styles/main.css:18-45`** — `:root` reorganised with grouped comment headings. Reads like a documented contract. Three new tokens follow `--ak-*` namespace.
- **Layering direction is clean** — tokens → CSS classes/layouts → slides.md/components. No reverse dependency.
- **Boundary respected for new visual primitives** — `.color-chip`, `.rgb-demo`, `.diagram-img`, `.breadboard-diagram` defined in main.css, consumed by class name. Old inline style hacks replaced.
- **`.color-chip` API design is solid** — `var(--bg)`/`var(--fg)` as instance variables, consumer passes them inline. Textbook CSS-custom-property-as-component-API pattern. Pedagogiskt skyddade konstanter stay at consumer end.
- **Atomic-commit hygiene is excellent** for `git bisect` — 16 logical commits in implementation order matching the design spec §13.
- **`presentation/components/WiringSlide.vue:28`** — Uses existing arbitrary-Tailwind pattern, keeps component style discipline.
- **Print-overrides simplification (`main.css:302-313`)** — Removed redundant `text-shadow: none` rule (centralisation pays off).

### Issues

#### Critical (must fix)
None.

#### Important (should fix)

**1. CLAUDE.md documents a non-existent `.hex-chip` class**
- **Where:** `CLAUDE.md:64` lists `.hex-chip` in Custom CSS-klasser table.
- **What:** Class does not exist (verified via grep). Actual added class is `.color-chip` (line 58).
- **Fix:** Delete the `.hex-chip` row in `CLAUDE.md:64`. Without fix, future sessions hunt for code that doesn't exist.

**2. `voltage-divider.svg` hex codes not linked to CSS tokens**
- **Where:** `presentation/public/images/voltage-divider.svg`
- **What:** Hex codes in SVG mean any future token rename will silently break SVG-text contrast.
- **Fix (forward-looking, not blocking):** Add `<!-- Token mirror: ak-accent #00747d, ak-text #0a0e14, ... -->` comment at top of SVG. Costs nothing; gives `git grep #00747d` enough context.

**3. `.rgb-demo` mixes presentation-only colour with the variable system**
- **Where:** `main.css:278-284`: `background: #000`
- **What:** `#000` is intentional (per design spec — black frame meeting black image edge).
- **Recommendation:** Keep `#000` literal but add a one-line comment explaining the pedagogical "screen" metaphor.

**4. Scope artefacts — PNG/PDF rendering and CLAUDE.md beyond plan**
- **What:** Three commits extend beyond the 18-task plan:
   - `a1ac16f` (52 PNGs + PDF binary, ~12 MB)
   - `4eb8402` (overlapping PDF binary)
   - `d3dd82e` (CLAUDE.md, 112 lines)
- **PNG+PDF matches §10A "Verifierings-plan"** — plan-consistent.
- **CLAUDE.md is a legitimate post-impl learning capture.**
- **Concerns:**
   - `tasks/light-mode-rendered/` checked in as binary artefacts. Consider `.gitignore` or document as frozen review reference.
   - `4eb8402` and `a1ac16f` may overlap. Verify with `git show 4eb8402 --stat`. If duplicated, squash before merge.

#### Minor (nice to have)

**1. `breadboard-diagram` and `rgb-demo` are slide-singleton classes** — Used once each. OK for current deck; flag if presentation/ ever gets a second deck.

**2. `:root` tokens organised by domain but property names don't reflect domain** — `--ak-glow-cyan` and `--ak-glow-red` lost literal-color truth (now teal/rose). Renaming to `--ak-glow-accent` / `--ak-glow-danger` would align names. Sweep when next touching tokens.

**3. `colorSchema: 'light'` is single-quoted in slides.md** — Matched style would be unquoted. Slidev accepts both. Style consistency point.

**4. PDF-export-overrides comment trimmed too tersely** — Old comment had the rationale (Slidev's `?print=1`, `pages/print.vue`, Chromium banding); new is one line. CLAUDE.md captures part but not at the right place.

**5. `.diagram-img` sits alongside `.rgb-demo` and `.breadboard-diagram` but is more generic** — Naming inconsistency. Should be `.pwm-waveform` for symmetry, or the others should be renamed generic.

### Assessment

**Ready to merge?** Yes, with fixes.

**Reasoning:** The structural choices are coherent. Token architecture, component boundaries, dependency direction, and atomic commit ordering are correct per spec. Two real concerns are documentation drift (`.hex-chip` ghost) and scope artefacts (binary PNG/PDF, redundant PDF commits). Both are 5-minute fixes.

### Out of scope but flagging:

- **WCAG verification with `--ak-text-mute`**
- **`.cyan` utility class name vs `--ak-accent` token** — Class name is now misleading (value is teal `#00747d`). Same vestigial-naming issue. Coherent rename if combined with `--ak-glow-cyan` rename.
- **Code-block `.cyan` colour application in inline `<code>`** — Verify two color sources for code don't clash.
- **Browser-export vs CLI-export pipeline** — If "premium" cover-with-hero PDFs are needed regularly, CI-friendly path needed.
